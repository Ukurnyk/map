import css from "./Info.module.css";

const Info = (props) => {
  return (
    <div className={css.info}>
      <h2 className={css.title}>Страна1</h2>
      <p className={css.text}>ывфывыфвыфвфывфыв</p>
    </div>
  );
};

export default Info;
